# Secrets — never commit
.env
.env.local

# Node
node_modules/
npm-debug.log*
.npm/

# Astro
frontend/dist/
frontend/.astro/

# Python (phases 3–4)
__pycache__/
*.py[cod]
.venv/
venv/

# Code-review hook scratch state (per-session queues and once-per-turn flags).
# The hooks and the agent definition ARE tracked; only this runtime state is not.
.claude/.review/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp

# Note: frontend/src/data/github-cache.json is intentionally NOT ignored.
# It is a committed build artifact so builds are reproducible offline — see
# decisions/0002-github-as-enrichment-not-source.md
